<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <title><%= title || "Register | MindWell" %></title>

  <!-- Bootstrap -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />

  <!-- Font Awesome -->
  <link
    href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
    rel="stylesheet"
  />

  <style>
    body {
      background-color: #f8f9fa;
    }

    .register-card {
      width: 380px;
      border-radius: 18px;
    }

    .btn-primary {
      background: linear-gradient(90deg, #0d6efd, #20c997);
      border: none;
    }

    .btn-primary:hover {
      opacity: 0.95;
    }

    .form-control {
      padding: 10px 14px;
    }
  </style>
</head>

<body>

<div class="min-vh-100 d-flex align-items-center justify-content-center">

  <div class="card shadow-lg border-0 p-4 register-card bg-white">

    <!-- LOGO -->
    <div class="text-center mb-3">
      <i class="fa-solid fa-brain text-primary fs-2"></i>
      <h5 class="mt-2 fw-bold">MindWell</h5>
    </div>

    <h4 class="text-center fw-semibold mb-1">Create Account</h4>
    <p class="text-center text-muted mb-4" style="font-size: 0.9rem;">
      Join MindWell to track your mental wellness
    </p>

    <!-- 🔴 ERROR ALERT -->
    <% if (typeof error !== "undefined" && error) { %>
      <div class="alert alert-danger text-center py-2">
        <%= error %>
      </div>
    <% } %>

    <form method="POST" action="/register" novalidate>

      <div class="mb-3">
        <label class="form-label fw-medium">Email</label>
        <input
          type="email"
          class="form-control"
          name="email"
          placeholder="you@example.com"
          required
        />
      </div>

      <div class="mb-3">
        <label class="form-label fw-medium">Password</label>
        <input
          type="password"
          class="form-control"
          name="password"
          placeholder="••••••••"
          required
        />
      </div>

      <button type="submit" class="btn btn-primary w-100 py-2 fw-semibold mt-2">
        Create Account
      </button>
    </form>

    <p class="text-center mt-3 mb-0" style="font-size: 0.9rem;">
      Already have an account?
      <a href="/login" class="fw-semibold text-decoration-none">Login</a>
    </p>

  </div>

</div>

</body>
</html>
